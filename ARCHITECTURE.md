# 구조

## 왜 이렇게 만들었나

비짓서울 API는 관광 콘텐츠를 잘 갖추고 있지만, **"지금 갈 수 있는가"라는 질문에는
그대로 답하지 못한다.** 세 가지가 비어 있기 때문이다.

| 공백 | 실측 | 결과 |
|---|---|---|
| 행사 기간이 목록 응답에 없다 | 상세 조회에만 `schdul_info_bgnde/endde` | 목록만 쓰면 끝난 행사를 추천하게 된다 |
| 운영시간이 자유 문장이다 | 규칙으로 확정 가능 **22.9%** | 오늘 여는지 기계가 판정할 수 없다 |
| 실내·실외 필드가 없다 | 규칙으로 판정 불가 **24.8%** | 날씨 대응의 전제가 없다 |

이 저장소는 그 공백을 메우는 계층이다. 여기에 **'요즘 뜨는가'와 '지금 붐비는가'를
따로 재는 계층**을 더해, 통과한 것만으로 일정을 짠다.

## 파이프라인

```
        ┌──────────┐   ┌───────────┐   ┌──────────┐   ┌────────┐
 API →  │ collect  │ → │ normalize │ → │ validate │ → │ course │ → UI
        └──────────┘   └───────────┘   └──────────┘   └────────┘
             │              │               │              │
        data/raw/*     구조화 시간표      통과/탈락/불가    반나절 코스
                       실내외 태깅
```

### collect — 수집

`sources/`에 두 구현이 같은 인터페이스(`count` / `list_ids` / `fetch`)를 갖는다.

- `CatalogSource` 공개 카탈로그(HTML). 키 없이 동작한다
- `ApiSource` 공식 API. `VISITSEOUL_API_KEY` 필요

**목록 조회에는 좌표·기간·운영시간이 없다.** 상세 조회를 건건이 해야 하므로
실시간 호출이 아니라 사전 배치가 필수 설계다. 수집은 `cid` 기준으로 재개
가능해서 중단해도 이어받는다.

### normalize — 정규화

`parse_hours()`는 "화~금요일 12:15~22:00 토요일 : 12:00~22:00"처럼 한 줄에 여러
요일 구간이 섞인 문장을 요일별 시간표로 바꾼다. 핵심은 **실패를 숨기지 않는
것**이다.

| confidence | 뜻 |
|---|---|
| `high` | 요일·시각이 모두 명시되어 그대로 판정 가능 |
| `low` | 파싱은 됐지만 예외 단서(`※`, "프로그램별 상이")가 있거나 요일을 가정함 |
| `none` | 시각 패턴 자체가 없음 |

`low`와 `none`의 합이 곧 LLM이 담당해야 할 몫이고, 그 비율이 제안서의 근거다.

### validate — 판정

기간 → 날씨 → 운영 순으로 거른다. **탈락(`False`)과 판정 불가(`None`)를
구분하는 것이 이 모듈의 전부**라고 해도 된다. 정보가 없어서 모르는 것을
"열려 있다"로 처리하면 헛걸음이 생기고, "닫혔다"로 처리하면 멀쩡한 후보가
사라진다. 어느 쪽도 사용자에게 정직하지 않다.

### course — 구성

첫 장소가 나머지 일정의 위치를 정한다. 그래서 앵커만은 순위(거리·품질·인기·
취향)를 제대로 세우고, **7일 안에 끝나는 행사만** 그 순위를 뛰어넘어 앞으로
당긴다. 끝이 임박했다는 것은 취향보다 우선하는 정보지만 — 다시 올 기회가
없으니까 — 두 달 남은 전시까지 그 특권을 줄 이유는 없다. 행사를 무조건
앵커로 두던 초기 구현은 "쇼핑을 보고 싶다"고 말해도 전시로 시작했다.

앵커 주변 도보권에서 로컬 음식과 실내 대안을 붙인다.

연중 상시 영업을 `2026.01.01~12.31`로 적어 둔 식당이 많아, 기간이 있다는 것만으로
행사로 보지 않는다(`is_short_event`, 90일 이하).

### popularity — 인기 지표

키 없이 쓸 수 있는 실측 신호는 위키백과 조회수뿐이다(카카오·네이버 API는
평점을 주지 않는다). 다만 이름으로 문서를 붙이면 매칭 954건 중 650건이
다른 대상이 된다 — 종각의 카레집이 히말라야산맥 문서에 붙는 식이다.
문서 좌표와 장소 좌표의 거리로 걸러 내고, 검증 결과는 지우지 않고
`geo_ok`로 표시만 해 다시 돌릴 수 있게 둔다.

### momentum — 추세 엔진

**자료의 뜻을 모르는 것이 설계다.** `axes(values, period, scale)`은 숫자 배열만
받고, 그것이 조회수인지 유동인구인지 소비액인지 모른다. 소스는 함수 하나를
`@source(...)`로 등록해 꽂는다. 유행이 바뀌어도 엔진은 남는다.

세 축으로 쪼개고 **합치지 않는다.** 하나로 뭉치면 크기가 압도적인 곳이 언제나
이겨 변화가 묻힌다.

| 축 | 계산 |
|---|---|
| 수준 | 최근 12개월 합 |
| 모멘텀 | 전년 **동월**비 |
| 급등 | 최근 3개월 z점수 |

계절성이 지배적이라 비교 시차는 반드시 **주기와 같아야** 한다. 초기 구현은
`min(12, n//2)`를 썼는데, 20개월짜리 계열이 10개월 전과 비교되어 계절 성분이
그대로 남았다. 지금은 `n < 2*period`면 아예 `None`을 준다 — 모자란 자료로
계산해 주는 것보다 못 한다고 말하는 편이 낫다.

두 보정을 건다.

- **시장 효과 제거** — 각 계열의 전년비에서 그 소스 전체의 **중앙값**을 뺀다
  (변이할당분석). 평균이 아니라 중앙값인 이유는 청와대(+419%) 같은 한 건이
  기준선을 끌고 가기 때문이다.
- **크기 보정** — 셈 자료에는 분산 안정화 변환 `A(x)=2√(x+3/8)`을 적용한다.
  분모가 작을수록 비율이 쉽게 커지는 것을 눌러, 월 1건짜리와 500건짜리를
  같은 자에 올린다.

이상치 상한은 비율 6.0배다. 남산서울타워가 27.3배(+2,625%)로 나왔는데 장소가
뜬 게 아니라 문서명이 바뀌어 조회수가 합쳐진 인공물이었다. 27.3배는 혼자
떨어져 있고 그다음이 5.2배라, 그 사이에 선을 그었다.

### crowd — 지금 붐비는가

추세는 '요즘 뜨는가'를 말하고 이건 '지금 붐비는가'를 말한다. 둘은 다른 질문이고,
이 서비스가 하려는 말은 그 교차점에 있다.

관측 지역 반경 **800m 밖에는 값을 쓰지 않는다.** 남의 동네 혼잡을 이 자리의
혼잡이라 말하면 안 된다. 캐시는 5분 — 원자료가 그 주기로 갱신된다.

붐빈다고만 말하고 끝내면 갈 곳을 잃는다. 12시간 예보에서 **처음 여유해지는
시각**을 찾아 함께 준다. 다만 가라 마라를 정해 주지는 않는다 — 붐비는 곳을
굳이 찾아가는 이유가 사람마다 다르고, 그걸 대신 정해 줄 근거가 없다.

### i18n — 경계에서만 옮긴다

**비짓서울 원문은 손대지 않는다.** 영업시간 원문을 어설프게 옮기면 없는 정보를
만든다. 우리가 쓴 문장만, 응답이 나가는 경계에서 옮긴다. 표에 없는 한국어는
지우지 않고 그대로 남긴다 — 조용히 사라지는 것보다 남아 있는 편이 고치기 쉽다.

한국어는 낱말 사이에 경계 표시가 없어 부분 문자열이 그대로 걸린다. `도보`가
`듣도보도못한말` 안에서 잡혀 `듣Walk도못한말`이 됐고, `붐빔`이 `붐빔은`에서
잡혀 `Crowded은`이 됐다. **세 글자 이하 키는 앞뒤가 한글이면 건너뛴다.**

단위는 숫자가 앞에 붙을 때만 바꾸고, 구절보다 **먼저** 처리한다. 구절 표가
`조회`를 먼저 지우면 `588회 조회`를 한 덩어리로 볼 기회가 사라져
`588 views views`가 된다.

서버 표(`i18n.PHRASES`)와 화면 표(`app.js`의 `KO2EN`)가 갈라지면 영어 화면에
한국어가 새므로, 테스트가 둘의 일치를 강제한다.

### clock — '지금'은 서울의 지금

`datetime.now()`는 프로세스가 도는 기계의 지역시간을 준다. 배포처에 TZ가 없어
UTC로 돌았고, 서울 오후 4시를 오전 7시로 판정했다. '지금 갈 수 있는 곳'이
1,507건이어야 할 자리에 215건이 나왔다.

화면은 브라우저가 시각을 실어 보내 멀쩡했다. **시각을 안 보내는 경로에서만
조용히 틀렸다** — 통계, 챗봇 인사, 직접 부르는 API. 한국은 1988년 이후
서머타임이 없어 UTC+9가 언제나 정확하므로, tzdata 없이 고정 오프셋으로 둔다.

## 키가 없을 때

하나도 없어도 전부 돈다. 무엇이 쓰였는지는 응답에 항상 실린다.

| 키 | 없을 때 | 표시 |
|---|---|---|
| `VISITSEOUL_API_KEY` | 공개 카탈로그로 수집 | `Content.source` |
| `KMA_API_KEY` | 맑음 21°C 기본값 | `weather.source` = `fallback` |
| `TMAP_APP_KEY` 등 | 직선거리 추정 | `leg.provider` = `estimate`, `exact` = false |
| `SEOUL_RTD_KEY` | 혼잡 축이 중립(0.5) | `/api/health`의 `keys.seoul_rtd` |
| `ANTHROPIC_API_KEY` | 규칙 의도 파악·템플릿 답변 | `engine` = `rules` |

## 지리 정보

통계청 행정동 경계(EPSG:5186)에서 서울 426개만 뽑아 WGS84로 변환한다.
자치구는 행정동코드 앞 5자리로 매핑한다(`geo.GU_BY_PREFIX`).

지도 배경 외에 쓰임이 하나 더 있다. 추천 결과가 어느 자치구에 떨어졌는지 세면
**오버투어리즘 분산 효과를 정량화**할 수 있다. `/api/stats`의 `distribution`이
그 기준선이다.

### chat — 대화

LLM은 **말을 알아듣고 말을 만드는 데만** 쓴다. 무엇을 추천할지는 판정 엔진이
정한다. LLM이 장소를 지어내면 "갔는데 없더라"가 다시 시작되기 때문이다.
의도 추출과 답변 생성 두 곳에만 관여하며, 키가 없으면 각각 규칙과 템플릿으로
떨어진다. 앞 턴의 조건을 물려받아 "밥 먹을 곳 위주로" 같은 이어지는 요청이 된다.

## 프런트엔드

빌드 단계가 없다. `web/`의 정적 파일을 FastAPI가 그대로 서빙한다.
Leaflet은 CDN 의존을 없애려고 `web/vendor/`에 동봉했다.

## 소요시간을 어떻게 재나

직선거리를 4km/h로 나눈 값은 "대충 이 정도"일 뿐이다. 한강을 건너거나 고가·철길에
막히면 실제 도보 시간은 두 배가 되기도 한다. 관광객에게 "도보 4분"이라고 말하려면
실제 경로를 물어야 한다.

| 수단 | 제공자 | 환경변수 |
|---|---|---|
| 도보 | TMAP 보행자 경로 | `TMAP_APP_KEY` |
| 대중교통 | ODsay | `ODSAY_API_KEY` |
| 자동차 | 네이버 Directions 5 | `NAVER_CLIENT_ID` / `NAVER_CLIENT_SECRET` |
| 없으면 | 직선거리 × 우회율 1.3 | 키 불필요 |

네이버 지도 API에는 보행자·대중교통 경로가 공개돼 있지 않아 자동차만 맡는다.
도보는 TMAP, 대중교통은 ODsay가 국내에서 가장 널리 쓰이는 조합이다.

구간이 900m 이하면 대중교통을 아예 조회하지 않는다. 대기와 환승 때문에 짧은
거리에서는 걷는 편이 빠르기 때문이다. 각 구간에는 실측인지 추정인지가 함께
표시된다 — 추정값을 실측처럼 보이게 하면 그것도 일종의 헛걸음이다.

## 화면

지도가 화면 전체를 채우고 그 위에 유리판이 뜬다. 관광 서비스이므로 밝은 낮의
톤을 쓴다. 배경은 하늘에서 가져온 그라데이션이고, 패널은 `backdrop-filter`로
지도를 비친다.

OpenStreetMap 표준 타일을 쓰되 CSS에서 채도를 한 겹 눌렀다. 원본은 색이 강해
유리판 위 글씨를 방해한다. Esri Light Gray Canvas도 검토했지만 국내에서는
축척 14 이상에 데이터가 없어(빈 타일에 "Map data not yet available") 쓸 수 없었다.

지도는 Leaflet이다. MapLibre는 WebGL을 요구하는데, 우리가 지도에 시키는 일은
래스터 타일·폴리곤·원형 마커·선이 전부라 Leaflet으로 충분하고 WebGL이 없는
환경에서도 그려진다.
